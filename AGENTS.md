# 个人知识库协作约定

此库是用户的 Obsidian 知识库。处理与此库有关的任务时，先读取：

1. `AI协作-通用记忆包/CURRENT.md`
2. `AI协作-通用记忆包/COLLABORATION_MEMORY.md`
3. `AI协作-通用记忆包/DECISIONS.md`

## 通用协作记忆的更新规则

- 只沉淀对未来协作有价值、可复用且已由用户确认或有明确证据支持的事实、偏好、流程和决策。
- 新观察先写进 `MEMORY_INBOX.md`；未经验证的猜测不得直接写入长期记忆。
- 每个完成的重要任务或明确的长期决策后，更新 `CURRENT.md`；必要时把验证后的条目提升到 `COLLABORATION_MEMORY.md` 或 `DECISIONS.md`。
- 不记录密码、令牌、Cookie、API Key、私钥、认证文件、完整聊天记录、无关隐私或敏感终端输出。
- 新证据推翻旧结论时保留历史，并标注“已废弃”和替代结论。

## 与运维实习教学的边界

- 运维实习的进度、每日一问、错题、教学策略和课程决策，以 `求职/运维实习-教师包/` 为准；不要复制到通用包。
- 遇到运维实习备战教学场景，继续遵循 `求职/运维实习-教师包/codex/AGENTS.md` 和其中引用的 `yunwei-teacher` 技能。
- 当前只自动进行三个月运维实习老师模式；长期学习教练和通用学习教练不自动启用，除非用户明确要求恢复或使用它们。
